<Window x:Class="UltScan.PinButtonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        AllowsTransparency="True"
        Background="Transparent"
        Width="28"
        Height="28">
        <Button x:Name="CloseButton"
                FontSize="14"
                Background="Transparent"
                BorderThickness="0"
                Click="CloseButton_Click"
                MouseEnter="CloseButton_MouseEnter"
                MouseLeave="CloseButton_MouseLeave">
        <Button.Style>
            <Style TargetType="Button">
                <Setter Property="Foreground" Value="#222"/>
                <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
                <Setter Property="Content" Value=""/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Root"
                                    Background="#DDFFFFFF"
                                    BorderBrush="#55000000"
                                    BorderThickness="1"
                                    CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter TargetName="Root" Property="Border.Background" Value="#FF3B30"/>
                                    <Setter TargetName="Root" Property="Border.BorderBrush" Value="#FF1F1F"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Root" Property="Border.Background" Value="#D72A24"/>
                                    <Setter TargetName="Root" Property="Border.BorderBrush" Value="#B01C18"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Button.Style>
    </Button>
</Window>
